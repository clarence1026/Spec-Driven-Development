meta:
  page: <page-name>
  route: <route-path>
  pattern: <pattern-id>
  goal: <page-goal>
  in_scope:
    - <in-scope-item>
  out_of_scope:
    - <out-of-scope-item>
  pattern_deviation_reason: <reason-if-any>

layout:
  sections:
    - id: <section-id>
      type: <section-type>
      purpose: <purpose>

states:
  page:
    - loading
    - ready
    - empty
    - error

interactions:
  - id: <interaction-id>
    trigger: <trigger>
    result: <result>
    feedback: <feedback>
    fallback: <fallback>

displayRules:
  - field: <field-name>
    rule: <display-rule>

constraints:
  - <constraint>

responsive:
  desktop:
    <section-id>: <desktop-rule>
  pad:
    <section-id>: <pad-rule>
  mobile:
    <section-id>: <mobile-rule>

acceptance:
  - <acceptance-rule>
