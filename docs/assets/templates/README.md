# 模板目录

当前默认执行入口已经切换到 superpowers-first，因此当前仓库中的“模板”主要以：

- `docs/superpowers/spec-template.md`
- `docs/superpowers/plan-template.md`

为主。

`docs/quickstart/templates/` 仍然保留，但仅作为：

- 历史逻辑参考
- 未来 schema / workflow 抽象参考
- 对比当前执行入口的辅助材料

当稳定字段被多个项目持续复用后，再逐步从当前 superpowers 模板骨架提升为正式共享模板或结构化 schema。
