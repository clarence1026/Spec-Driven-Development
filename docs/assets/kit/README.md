# Kit 目录

当前目录预留给 `Kit（组件组合包）`，也就是可复用组件组合，例如：

- 列表页的 filter + table + pagination 组合
- 营销首页的 hero + section + cta 组合
- 已在多个页面中共同出现的区块组合

可以先这样理解：

`Kit（组件组合包） = 一组经常一起出现、可以成套复用的组件组合`

当前阶段，Kit 的优先级排在 Starter 之后。

只有当某组组件组合已经在多个页面中被证明可复用时，再逐步进入本目录。
